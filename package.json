{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Servidor mock para múltiples IP/puerto y protocolos (ISO8583, WebService, REST)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.2",
    "@tikpay/iso8583-ts": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.2"
  }
}
